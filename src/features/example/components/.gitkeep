# This file only keeps this folder in git until you add a real component.
# A feature's OWN components go here -- forms don't; every form (and the
# shared field infrastructure) lives in src/forms/ instead, see README.md
# §11. See README.md §3 for the full "where things go" decision table.
